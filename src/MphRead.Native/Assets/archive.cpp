/*
 * The SNDFILE archive implementation belongs to the one-to-one
 * MphRead/Utility/Archive.cs port in Utility/Archive.cpp.
 *
 * This Assets-side translation unit is retained only because the native
 * build lists the historical helper path; it intentionally owns no second
 * archive implementation.
 */
