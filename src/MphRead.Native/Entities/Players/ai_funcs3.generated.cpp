// Generated by tools/gen-ai-methods.py from
// src/MphRead/Entities/Players/PlayerAi.cs.  Do not edit by hand.
//
// PlayerAi's Func3_* predicates: the questions a bot asks
// to weigh one path through its personality tree against
// another.  Each answers 1 or 0.
#include "Entities/Players/PlayerAi.hpp"

#include "Utility/rng.hpp"

namespace fruityprime::players {
namespace {

template <typename Flags>
[[nodiscard]] constexpr Flags without(Flags value,
                                      Flags bit) noexcept {
    return static_cast<Flags>(
        static_cast<std::uint32_t>(value)
        & ~static_cast<std::uint32_t>(bit));
}


// A Data5 parameter is a fixed-point number wherever it is
// compared against a position.
[[nodiscard]] constexpr float param_float(
    std::int32_t value) noexcept {
    return static_cast<float>(value) / 4096.0F;
}

} // namespace

using utility::get_random_int2;

int PlayerAiData::func3_213A650(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::Bit13) ? 1 : 0;
}

int PlayerAiData::func3_213A660(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return parameters.param1 + static_cast<int>(
        utility::get_random_int2(
            parameters.param2 - parameters.param1));
}

int PlayerAiData::func3_213A688(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::AiStart) ? 1 : 0;
}

int PlayerAiData::func3_213A698(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if ((_scene.RoomId == 114 || _scene.RoomId == 112) && _player.Speed != Vector3.Zero && !_player.IsAltForm)
    // {
    // return 0;
    // }
    // return _player._standTerrain == Terrain.Lava ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213A714(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player._horizColTimer > 10 * 2 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213A72C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // if (other != _player && other.IsBot && other.AiData.Flags2.TestFlag(AiFlags2.Bit19))
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213A798(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // if (other != _player && other.IsBot && other.AiData.Flags2.TestFlag(AiFlags2.Bit18))
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213A804(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213A828(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213A828(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player.Flags1.TestFlag(PlayerFlags1.UsedJump) ? 0 : 1;
    return 0;
}

int PlayerAiData::func3_213A844(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213A868(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213A868(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player._timeSinceGrounded > 30 * 2 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213A884(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213A8A8(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213A8A8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // return _targetPlayer._deathaltTimer != 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213A8DC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213A900(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213A900(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player._deathaltTimer != 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213A91C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags4_, AiFlags4::Bit2) ? 0 : 1;
}

int PlayerAiData::func3_213A938(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags4_, AiFlags4::Bit2) ? 1 : 0;
}

int PlayerAiData::func3_213A94C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 31;
    // }
    // if (GameState.RadarPlayers)
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // if (_targetPlayer.Flags2.TestFlag(PlayerFlags2.RadarReveal) || _targetPlayer.OctolithFlag != null || _targetPlayer.IsPrimeHunter)
    // {
    // return 0;
    // }
    // return 31 - (int)(_targetPlayer.CurAlpha * 31);
    return 0;
}

int PlayerAiData::func3_213A9B8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // if (GameState.RadarPlayers)
    // {
    // return 31;
    // }
    // Debug.Assert(_targetPlayer != null);
    // if (_targetPlayer.Flags2.TestFlag(PlayerFlags2.RadarReveal) || _targetPlayer.OctolithFlag != null || _targetPlayer.IsPrimeHunter)
    // {
    // return 31;
    // }
    // return (int)(_targetPlayer.CurAlpha * 31);
    return 0;
}

int PlayerAiData::func3_213AA20(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && Func3_213AA64(context, param) == 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AA64(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // float dist = param.Param1 / 4096f;
    // return _targetPlayer.Position.LengthSquared < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AAF0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.Z < param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AB24(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.Z > param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AB58(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.X < param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AB8C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.X > param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213ABC0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.Y >= param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AC04(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetPlayer) && _targetPlayer != null
    // && _targetPlayer.Position.Y < param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213AC38(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.z < param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213AC54(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.z > param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213AC70(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.x < param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213AC8C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.x > param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213ACA8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.y >= param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213ACCC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).position.y < param_float(parameters.param1) ? 1 : 0;
}

int PlayerAiData::func3_213ACE8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (GameState.Mode == GameMode.Capture && _player.TeamIndex == 0 && _field30 > 100)
    // {
    // return 1;
    // }
    // int navIndex = _nodeTypeIndex[(int)NodeType.Navigation];
    // int specIndex = _nodeTypeIndex[(int)NodeType.Special];
    // if (navIndex >= specIndex)
    // {
    // return 1;
    // }
    // while (navIndex < specIndex)
    // {
    // NodeData3 node = _nodeList[navIndex];
    // if (node.Field4 == _field30)
    // {
    // return 0;
    // }
    // navIndex++;
    // }
    // return 1;
    return 0;
}

int PlayerAiData::func3_213AD64(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213AD88(session, bot_slot, context,
                      parameters) ^ 1;
}

int PlayerAiData::func3_213AD88(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return field30_ == parameters.param1 ? 1 : 0;
}

int PlayerAiData::func3_213ADA0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213ADC4(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213ADC4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return GameState.Mode == GameMode.PrimeHunter && _player.IsPrimeHunter ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213ADF8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.match_mode() != 7 ? 1 : 0;
}

int PlayerAiData::func3_213AE14(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.match_mode() == 7 ? 1 : 0;
}

int PlayerAiData::func3_213AE30(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 6 ? 1 : 0;
}

int PlayerAiData::func3_213AE48(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 6 ? 1 : 0;
}

int PlayerAiData::func3_213AE60(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 3 ? 1 : 0;
}

int PlayerAiData::func3_213AE78(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 3 ? 1 : 0;
}

int PlayerAiData::func3_213AE90(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 2 ? 1 : 0;
}

int PlayerAiData::func3_213AEA8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 2 ? 1 : 0;
}

int PlayerAiData::func3_213AEC0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 4 ? 1 : 0;
}

int PlayerAiData::func3_213AED8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 4 ? 1 : 0;
}

int PlayerAiData::func3_213AEF0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 5 ? 1 : 0;
}

int PlayerAiData::func3_213AF08(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 5 ? 1 : 0;
}

int PlayerAiData::func3_213AF20(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 1 ? 1 : 0;
}

int PlayerAiData::func3_213AF38(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 1 ? 1 : 0;
}

int PlayerAiData::func3_213AF50(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) != 0 ? 1 : 0;
}

int PlayerAiData::func3_213AF68(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player_hunter(bot_slot) == 0 ? 1 : 0;
}

int PlayerAiData::func3_213AF80(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ != 2 ? 1 : 0;
}

int PlayerAiData::func3_213AFA0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ == 2 ? 1 : 0;
}

int PlayerAiData::func3_213AFC0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ != 1 ? 1 : 0;
}

int PlayerAiData::func3_213AFE0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ == 1 ? 1 : 0;
}

int PlayerAiData::func3_213B000(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ != 0 ? 1 : 0;
}

int PlayerAiData::func3_213B020(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return bot_level_ == 0 ? 1 : 0;
}

int PlayerAiData::func3_213B040(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 8 ? 1 : 0;
}

int PlayerAiData::func3_213B058(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 8 ? 1 : 0;
}

int PlayerAiData::func3_213B070(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 7 ? 1 : 0;
}

int PlayerAiData::func3_213B088(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 7 ? 1 : 0;
}

int PlayerAiData::func3_213B0A0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 6 ? 1 : 0;
}

int PlayerAiData::func3_213B0B8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 6 ? 1 : 0;
}

int PlayerAiData::func3_213B0D0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 5 ? 1 : 0;
}

int PlayerAiData::func3_213B0E8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 5 ? 1 : 0;
}

int PlayerAiData::func3_213B100(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 4 ? 1 : 0;
}

int PlayerAiData::func3_213B118(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 4 ? 1 : 0;
}

int PlayerAiData::func3_213B130(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 3 ? 1 : 0;
}

int PlayerAiData::func3_213B148(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 3 ? 1 : 0;
}

int PlayerAiData::func3_213B160(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 2 ? 1 : 0;
}

int PlayerAiData::func3_213B178(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 2 ? 1 : 0;
}

int PlayerAiData::func3_213B190(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 1 ? 1 : 0;
}

int PlayerAiData::func3_213B1A8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 1 ? 1 : 0;
}

int PlayerAiData::func3_213B1C0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ != 0 ? 1 : 0;
}

int PlayerAiData::func3_213B1D8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return weapon2_ == 0 ? 1 : 0;
}

int PlayerAiData::func3_213B1F0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (NodeDefenseEntity defense in _scene.GetNodeDefenseEntities())
    // {
    // if (defense.CapturedPlayer == null || defense.CapturedPlayer.TeamIndex != _player.TeamIndex || defense.OccupiedBy != null)
    // {
    // return 0;
    // }
    // }
    // return 1;
    return 0;
}

int PlayerAiData::func3_213B260(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B284(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B284(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetDefense))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetDefense != null);
    // float dist = param.Param1 / 4096f;
    // return Vector3.DistanceSquared(_targetDefense.Position, _player.Position) < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B328(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B34C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B34C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (Flags2.TestFlag(AiFlags2.TargetDefense))
    // {
    // Debug.Assert(_targetDefense != null);
    // return _targetDefense.Contested ? 1 : 0;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213B37C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B3A0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B3A0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetDefense))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetDefense != null);
    // return _targetDefense.CapturedPlayer?.TeamIndex == _player.TeamIndex && _targetDefense.OccupiedBy != null ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B3F0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (NodeDefenseEntity defense in _scene.GetNodeDefenseEntities())
    // {
    // if (defense.CapturedPlayer?.TeamIndex == _player.TeamIndex && defense.OccupiedBy != null)
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213B45C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetDefense))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetDefense != null);
    // return _targetDefense.CapturedPlayer?.TeamIndex == _player.TeamIndex ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B4A0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Flags2.TestFlag(AiFlags2.TargetDefense) && Func3_213B4E4(context, param) == 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B4E4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetDefense))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetDefense != null);
    // return _targetDefense.Volume.TestPoint(_player.Volume.SpherePosition) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B528(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_flagBaseD8 != null);
    // Debug.Assert(_octolithFlagDC != null);
    // return Vector3.DistanceSquared(_flagBaseD8.Position, _player.Position)
    // < Vector3.DistanceSquared(_octolithFlagDC.Position, _player.Position) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B5DC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagDC != null);
    // Debug.Assert(_flagBaseD8 != null);
    // return Vector3.DistanceSquared(_octolithFlagDC.Position, _player.Position)
    // < Vector3.DistanceSquared(_flagBaseD8.Position, _player.Position) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B690(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (GameState.Mode == GameMode.Capture)
    // {
    // return 0;
    // }
    // Debug.Assert(_octolithFlagD4 != null);
    // Debug.Assert(_octolithFlagDC != null);
    // if (_octolithFlagD4.Carrier == null)
    // {
    // return 0;
    // }
    // return Vector3.DistanceSquared(_octolithFlagDC.BasePosition, _player.Position)
    // < Vector3.DistanceSquared(_octolithFlagD4.Carrier.Position, _player.Position) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213B77C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B7A0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B7A0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagD4 != null);
    // Debug.Assert(_flagBaseD8 != null);
    // if (_octolithFlagD4.Carrier != null)
    // {
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // if (other != _player && other.TeamIndex != _octolithFlagD4.Carrier.TeamIndex
    // && Vector3.DistanceSquared(_flagBaseD8.Position, other.Position) < 3 * 3)
    // {
    // return 1;
    // }
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213B88C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B8B0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B8B0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_flagBaseD8 != null);
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // if (other != _player && other.TeamIndex == _player.TeamIndex
    // && Vector3.DistanceSquared(_flagBaseD8.Position, other.Position) < 3 * 3)
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213B978(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213B99C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213B99C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_flagBaseD8 != null);
    // return Vector3.DistanceSquared(_flagBaseD8.Position, _player.Position) < 3 * 3 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BA04(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BA28(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BA28(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagD4 != null);
    // return _octolithFlagD4.AtBase ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BA44(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BA68(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BA68(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagDC != null);
    // return Vector3.DistanceSquared(_octolithFlagDC.Position, _player.Position) < 3 * 3 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BAD0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BAF4(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BAF4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagCC != null);
    // float dist = param.Param1 / 4096f;
    // return Vector3.DistanceSquared(_octolithFlagCC.Position, _player.Position) < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BB7C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BBA0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BBA0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagDC != null);
    // PlayerEntity? carrier = _octolithFlagDC.Carrier;
    // return carrier != null && carrier.Health != 0 && carrier.TeamIndex == _player.TeamIndex && carrier != _player ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BBE8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BC0C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BC0C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagD4 != null);
    // PlayerEntity? carrier = _octolithFlagD4.Carrier;
    // return carrier != null && carrier.Health != 0 && carrier.TeamIndex != _player.TeamIndex ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BC4C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BC70(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BC70(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_octolithFlagDC != null);
    // return _octolithFlagDC.Carrier == _player ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BC8C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BCB0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BCB0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player.Flags2.TestFlag(PlayerFlags2.AltAttack) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BCC4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213BCE8(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213BCE8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return CanChargeWeapon() ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BD7C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // FindEntityRef(AiEntRefType.Type57);
    // return _entityRefs.Field57 != null && Func3_213BE10(context, param) == 1 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BDF4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)BeamType.Missile];
    // return _player._ammo[info.AmmoType] >= info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BE10(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)BeamType.Missile];
    // return _player._ammo[info.AmmoType] < info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BE48(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    find_entity_ref(AiEntRefType::Type56);
    return (entity_refs_.entity(56) >= 0) && (func3_213BEA0(session, bot_slot, context, parameters) == 1) ? 1 : 0;
}

int PlayerAiData::func3_213BEA0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.inventory(bot_slot).available_weapons[1] ? 0 : 1;
}

int PlayerAiData::func3_213BEBC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.inventory(bot_slot).available_weapons[1] ? 1 : 0;
}

int PlayerAiData::func3_213BED8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    if (weapon1_ == 1)
    {
        find_entity_ref(AiEntRefType::Type57);
        return entity_refs_.entity(57) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 2)
    {
        find_entity_ref(AiEntRefType::Type59);
        return entity_refs_.entity(59) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 3)
    {
        find_entity_ref(AiEntRefType::Type61);
        return entity_refs_.entity(61) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 4)
    {
        find_entity_ref(AiEntRefType::Type63);
        return entity_refs_.entity(63) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 5)
    {
        find_entity_ref(AiEntRefType::Type65);
        return entity_refs_.entity(65) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 6)
    {
        find_entity_ref(AiEntRefType::Type67);
        return entity_refs_.entity(67) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 7)
    {
        find_entity_ref(AiEntRefType::Type69);
        return entity_refs_.entity(69) >= 0 ? 1 : 0;
    }
    if (weapon1_ == 8)
    {
        find_entity_ref(AiEntRefType::Type71);
        return entity_refs_.entity(71) >= 0 ? 1 : 0;
    }
    return 0;
}

int PlayerAiData::func3_213BFD8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)GetBeamType(_weapon2)];
    // return _player._ammo[info.AmmoType] >= info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213BFFC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)GetBeamType(_weapon2)];
    // return _player._ammo[info.AmmoType] < info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213C054(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)GetBeamType(_weapon1)];
    // return _player._ammo[info.AmmoType] >= info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213C078(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // WeaponInfo info = Weapons.Current[(int)GetBeamType(_weapon1)];
    // return _player._ammo[info.AmmoType] < info.AmmoCost ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213C0D0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    if (find_weapon_index_ == 1)
    {
        if (session.inventory(bot_slot).available_weapons[1])
        {
            find_entity_ref(AiEntRefType::Type57);
            return entity_refs_.entity(57) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type56);
        return entity_refs_.entity(56) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 2)
    {
        if (session.inventory(bot_slot).available_weapons[2])
        {
            find_entity_ref(AiEntRefType::Type59);
            return entity_refs_.entity(59) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type58);
        return entity_refs_.entity(58) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 3)
    {
        if (session.inventory(bot_slot).available_weapons[3])
        {
            find_entity_ref(AiEntRefType::Type60);
            return entity_refs_.entity(60) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type61);
        return entity_refs_.entity(61) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 4)
    {
        if (session.inventory(bot_slot).available_weapons[4])
        {
            find_entity_ref(AiEntRefType::Type63);
            return entity_refs_.entity(63) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type62);
        return entity_refs_.entity(62) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 5)
    {
        if (session.inventory(bot_slot).available_weapons[5])
        {
            find_entity_ref(AiEntRefType::Type65);
            return entity_refs_.entity(65) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type64);
        return entity_refs_.entity(64) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 6)
    {
        if (session.inventory(bot_slot).available_weapons[6])
        {
            find_entity_ref(AiEntRefType::Type67);
            return entity_refs_.entity(67) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type66);
        return entity_refs_.entity(66) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 7)
    {
        if (session.inventory(bot_slot).available_weapons[7])
        {
            find_entity_ref(AiEntRefType::Type69);
            return entity_refs_.entity(69) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type68);
        return entity_refs_.entity(68) >= 0 ? 1 : 0;
    }
    if (find_weapon_index_ == 8)
    {
        if (session.inventory(bot_slot).available_weapons[8])
        {
            find_entity_ref(AiEntRefType::Type71);
            return entity_refs_.entity(71) >= 0 ? 1 : 0;
        }
        find_entity_ref(AiEntRefType::Type70);
        return entity_refs_.entity(70) >= 0 ? 1 : 0;
    }
    return 0;
}

int PlayerAiData::func3_213C310(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213C334(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213C334(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (_findWeaponIndex >= 0 && _findWeaponIndex <= 8)
    // {
    // return _player._availableWeapons[GetBeamType(_findWeaponIndex)] ? 1 : 0;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213C470(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213C48C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213C48C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // Debug.Assert(_targetPlayer != null);
    // _targetPlayer.GetPosition(out Vector3 targetPos);
    // targetPos = targetPos.AddY(_targetPlayer.IsAltForm ? _targetPlayer.Values.AltColYPos : 0.5f);
    // if (Func3_2139A1C(context, targetPos, angleCos: 0.5f, maxDistSqr: 15 * 15))
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213C52C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // if (other != _player && other.TeamIndex != _player.TeamIndex && other.Health != 0
    // && other.Hunter == Hunter.Weavel && other.Halfturret.Health != 0)
    // {
    // return AggroFunc2148394(5, 7, 1, null, null) > 0 ? 1 : 0;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213C600(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player.Hunter == Hunter.Sylux && _executionTree[context.Depth + 1].CallCount > (120 * 2) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213C64C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (_player.Hunter == Hunter.Sylux)
    // {
    // return 100000 * _slotDamage[_player.SlotIndex] / 25;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213C698(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (_player.Hunter == Hunter.Spire)
    // {
    // return _player._health < (GameState.EncounterState[_player.SlotIndex] == 4 ? 270 : 500) ? 1 : 0;
    // }
    // if (_player.Hunter == Hunter.Weavel)
    // {
    // return _player._health < 110 ? 1 : 0;
    // }
    // if (_player.Hunter == Hunter.Sylux)
    // {
    // return _player._health < 700 ? 1 : 0;
    // }
    // if (_player.Hunter == Hunter.Trace)
    // {
    // return _player._health < 590 ? 1 : 0;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213C75C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return static_cast<int>(damage_from_halfturret_);
}

int PlayerAiData::func3_213C764(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213C89C(context, param);
    return 0;
}

int PlayerAiData::func3_213C88C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return static_cast<int>(slot_damage_[bot_slot]);
}

int PlayerAiData::func3_213C89C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // int hits = 0;
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // float distSqr = Vector3.DistanceSquared(other.Position, _player.Position);
    // bool hasAggro = false;
    // if (distSqr >= 7 * 7)
    // {
    // hasAggro = AggroFunc214857C(6, 1, 2, null, other);
    // }
    // if (other != _player && other.TeamIndex == _player.TeamIndex && other.Health != 0
    // && (distSqr < 7 * 7 || hasAggro))
    // {
    // hits += _slotHits[other.SlotIndex];
    // }
    // }
    // return hits;
    return 0;
}

int PlayerAiData::func3_213C9C4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return static_cast<int>(slot_hits_[bot_slot]);
}

int PlayerAiData::func3_213C9D4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213CA2C(context, param);
    return 0;
}

int PlayerAiData::func3_213CA00(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213CA2C(context, param);
    return 0;
}

int PlayerAiData::func3_213CA2C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _executionTree[context.Depth + 1].CallCount > (param.Param1 * 2) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213CA58(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return field118_ > parameters.param1 * 2 ? 1 : 0;
}

int PlayerAiData::func3_213CA70(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return field118_ >= 302 ? 1 : 0;
}

int PlayerAiData::func3_213CA84(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213CAA8(session, bot_slot, context,
                      parameters) ^ 1;
}

int PlayerAiData::func3_213CAA8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    find_entity_ref(AiEntRefType::Type55);
    return entity_refs_.entity(55) >= 0 ? 1 : 0;
}

int PlayerAiData::func3_213CADC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // FindEntityRef(AiEntRefType.Type55);
    // if (_entityRefs.Field55 == null)
    // {
    // return 0;
    // }
    // float dist = param.Param1 / 4096f;
    // return Vector3.DistanceSquared(_entityRefs.Field55.Position, _player.Position) < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213CB8C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213CBB0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213CBB0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (Func2137860())
    // {
    // return 0;
    // }
    // foreach (ItemInstanceEntity item in _scene.GetItemInstanceEntities())
    // {
    // if (!IsItemNotNeeded(item.ItemType) && !Func21377FC(item))
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213CBC0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213CBE4(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213CBE4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // FindEntityRef(AiEntRefType.Type54);
    // if (_entityRefs.Field54 == null)
    // {
    // return 0;
    // }
    // float dist = param.Param1 / 4096f;
    // return Vector3.DistanceSquared(_entityRefs.Field54.Position, _player.Position) < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213CC94(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (_player._healthMax <= _player._health)
    // {
    // return 0;
    // }
    // return _player._healthMax - _player._health;
    return 0;
}

int PlayerAiData::func3_213CCB0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health;
}

int PlayerAiData::func3_213CCBC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health > parameters.param1 ? 1 : 0;
}

int PlayerAiData::func3_213CCD8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health < parameters.param1 ? 1 : 0;
}

int PlayerAiData::func3_213CCF4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health == session.inventory(bot_slot).health_max ? 0 : 1;
}

int PlayerAiData::func3_213CD18(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health == session.inventory(bot_slot).health_max ? 1 : 0;
}

int PlayerAiData::func3_213CD34(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health < session.inventory(bot_slot).health_max / 4 ? 0 : 1;
}

int PlayerAiData::func3_213CD58(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return session.player(bot_slot).health < session.inventory(bot_slot).health_max / 4 ? 1 : 0;
}

int PlayerAiData::func3_213CD74(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return _player.IsAltForm && _player.Halfturret.Health == 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213CDA4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return damage_from_halfturret_ != 0 ? 1 : 0;
}

int PlayerAiData::func3_213CDB8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity other in _scene.GetPlayerEntities())
    // {
    // float distSqr = Vector3.DistanceSquared(other.Position, _player.Position);
    // bool hasAggro = false;
    // if (distSqr >= 7 * 7)
    // {
    // hasAggro = AggroFunc214857C(6, 1, 2, null, other);
    // }
    // if (other != _player && other.TeamIndex == _player.TeamIndex && other.Health != 0
    // && (distSqr < 7 * 7 || hasAggro) && _slotHits[other.SlotIndex] != 0)
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213CEE8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213CF0C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213CF0C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // return AggroFunc214857C(4, 2, 1, _targetPlayer, null) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213CF7C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::Bit21) ? 0 : 1;
}

int PlayerAiData::func3_213CF94(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::Bit21) ? 1 : 0;
}

int PlayerAiData::func3_213CFA4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return slot_hits_[bot_slot] != 0 ? 1 : 0;
}

int PlayerAiData::func3_213CFC0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213CFDC(session, bot_slot, context,
                      parameters) ^ 1;
}

int PlayerAiData::func3_213CFDC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::TargetItem) ? 1 : 0;
}

int PlayerAiData::func3_213CFF4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213D010(session, bot_slot, context,
                      parameters) ^ 1;
}

int PlayerAiData::func3_213D010(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::TargetHalfturret) ? 1 : 0;
}

int PlayerAiData::func3_213D028(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213D044(session, bot_slot, context,
                      parameters) ^ 1;
}

int PlayerAiData::func3_213D044(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::TargetPlayer) ? 1 : 0;
}

int PlayerAiData::func3_213D05C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D078(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D078(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // return _targetPlayer._frozenTimer != 0 ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D0A8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D0C4(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D0C4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // return _targetPlayer.IsAltForm ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D0F4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func213995C(Fixed.ToFloat(3849), maxDistSqr: 5 * 5) ? 0 : 1;
    return 0;
}

int PlayerAiData::func3_213D128(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func213995C(Fixed.ToFloat(3849), maxDistSqr: 4 * 4) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D15C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D178(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D178(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity player in _scene.GetPlayerEntities())
    // {
    // if (player != _player)
    // {
    // if (AggroFunc214857C(6, 1, 2, null, player))
    // {
    // return 1;
    // }
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D218(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D234(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D234(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // return AggroFunc214857C(6, 2, 1, _targetPlayer, null) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D2A4(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D2C0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D2C0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (PlayerEntity player in _scene.GetPlayerEntities())
    // {
    // if (player != _player && player.IsBot)
    // {
    // if (AggroFunc214857C(6, 1, 2, null, player))
    // {
    // return 1;
    // }
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D36C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D388(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D388(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // PlayerEntity? player = null;
    // if (GameState.Mode == GameMode.SinglePlayer)
    // {
    // player = PlayerEntity.Main;
    // }
    // else
    // {
    // foreach (PlayerEntity entity in _scene.GetPlayerEntities())
    // {
    // player = entity;
    // break;
    // }
    // }
    // if (player == null)
    // {
    // return 0;
    // }
    // return AggroFunc214857C(6, 1, 2, null, player) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D418(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D43C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D43C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // foreach (BeamProjectileEntity beam in _scene.GetBeamProjectileEntities())
    // {
    // if (beam.Lifespan != 0 && beam.Beam == BeamType.OmegaCannon)
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D49C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D4C0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D4C0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // for (int i = 0; i < _targetPlayer.EquipInfo.Beams.Length; i++)
    // {
    // if (_targetPlayer.EquipInfo.Beams[i].Lifespan != 0)
    // {
    // return 1;
    // }
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D514(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D530(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D530(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func213842C() ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D540(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D564(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D564(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // Debug.Assert(_targetPlayer != null);
    // float dist = param.Param1 / 4096f;
    // return Vector3.DistanceSquared(_targetPlayer.Position, _player.Position) < dist * dist ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D608(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D624(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D624(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (!Flags2.TestFlag(AiFlags2.TargetPlayer))
    // {
    // return 0;
    // }
    // FindEntityRef(AiEntRefType.Type0);
    // NodeData3? node0 = _entityRefs.Field0;
    // Debug.Assert(node0 != null);
    // FindEntityRef(AiEntRefType.Type2);
    // NodeData3? node2 = _entityRefs.Field2;
    // Debug.Assert(node2 != null);
    // return node0 == node2 || IsNodeInRange(node2) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D6AC(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D6D0(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D6D0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // FindEntityRef(AiEntRefType.Type0);
    // Debug.Assert(_entityRefs.Field0 != null);
    // return IsNodeInRange(_entityRefs.Field0) ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D710(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D734(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D734(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (Flags2.TestFlag(AiFlags2.TargetDoor))
    // {
    // Debug.Assert(_targetDoor != null);
    // return _targetDoor.Flags.TestFlag(DoorFlags.Locked) ? 1 : 0;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D758(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // return Func3_213D77C(context, param) ^ 1;
    return 0;
}

int PlayerAiData::func3_213D77C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // if (Flags2.TestFlag(AiFlags2.TargetDoor))
    // {
    // Debug.Assert(_targetDoor != null);
    // return _targetDoor.Flags.TestFlag(DoorFlags.ShotOpen) ? 1 : 0;
    // }
    // return 0;
    return 0;
}

int PlayerAiData::func3_213D7A0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213D7D0(session, bot_slot, context,
                      parameters);
}

int PlayerAiData::func3_213D7B8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return func3_213D7D0(session, bot_slot, context,
                      parameters);
}

int PlayerAiData::func3_213D7D0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::TargetItem) ? 0 : 1;
}

int PlayerAiData::func3_213D7E8(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return 0;
}

int PlayerAiData::func3_213D7F0(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags2_, AiFlags2::Bit0) ? 1 : 0;
}

int PlayerAiData::func3_213D800(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return has_flag(flags4_, AiFlags4::Bit0) ? 1 : 0;
}

int PlayerAiData::func3_213D814(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_node40 != null);
    // return _node40.Position.Y - _player.Position.Y > param.Param1 / 4096f ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D83C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    // NOT PORTED.  The managed body has lines the converter did not
    // understand, or calls something that is itself not ported, and a
    // half-converted behaviour would run the parts it did understand
    // without the parts it did not.  The managed source follows.
    // Debug.Assert(_node3C != null);
    // return IsNodeInRange(_node3C) && _node40 == _node3C ? 1 : 0;
    return 0;
}

int PlayerAiData::func3_213D87C(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters) noexcept {
    static_cast<void>(session);
    static_cast<void>(bot_slot);
    static_cast<void>(context);
    static_cast<void>(parameters);
    return 1;
}

// PlayerAi.ExecuteFuncs3: which predicate each identifier asks.
int PlayerAiData::dispatch_funcs3(
    const gameplay::Session& session,
    std::uint8_t bot_slot,
    const AiContext& context,
    const ai::Parameters& parameters,
    int func_id,
    bool& handled) noexcept {
    handled = true;
    switch (func_id) {
    case 0:
        return func3_213D87C(session, bot_slot, context,
                  parameters);
    case 3:
        return func3_213D7F0(session, bot_slot, context,
                  parameters);
    case 4:
        return func3_213D800(session, bot_slot, context,
                  parameters);
    case 5:
        return func3_213D7E8(session, bot_slot, context,
                  parameters);
    case 6:
        return func3_213D7D0(session, bot_slot, context,
                  parameters);
    case 7:
        return func3_213D7B8(session, bot_slot, context,
                  parameters);
    case 8:
        return func3_213D7A0(session, bot_slot, context,
                  parameters);
    case 39:
        return func3_213D044(session, bot_slot, context,
                  parameters);
    case 40:
        return func3_213D028(session, bot_slot, context,
                  parameters);
    case 41:
        return func3_213D010(session, bot_slot, context,
                  parameters);
    case 42:
        return func3_213CFF4(session, bot_slot, context,
                  parameters);
    case 43:
        return func3_213CFDC(session, bot_slot, context,
                  parameters);
    case 44:
        return func3_213CFC0(session, bot_slot, context,
                  parameters);
    case 45:
        return func3_213CFA4(session, bot_slot, context,
                  parameters);
    case 49:
        return func3_213CF94(session, bot_slot, context,
                  parameters);
    case 50:
        return func3_213CF7C(session, bot_slot, context,
                  parameters);
    case 51:
        return func3_213CDA4(session, bot_slot, context,
                  parameters);
    case 53:
        return func3_213CD58(session, bot_slot, context,
                  parameters);
    case 54:
        return func3_213CD34(session, bot_slot, context,
                  parameters);
    case 55:
        return func3_213CD18(session, bot_slot, context,
                  parameters);
    case 56:
        return func3_213CCF4(session, bot_slot, context,
                  parameters);
    case 57:
        return func3_213CCD8(session, bot_slot, context,
                  parameters);
    case 58:
        return func3_213CCBC(session, bot_slot, context,
                  parameters);
    case 59:
        return func3_213CCB0(session, bot_slot, context,
                  parameters);
    case 66:
        return func3_213CAA8(session, bot_slot, context,
                  parameters);
    case 67:
        return func3_213CA84(session, bot_slot, context,
                  parameters);
    case 68:
        return func3_213CA70(session, bot_slot, context,
                  parameters);
    case 69:
        return func3_213CA58(session, bot_slot, context,
                  parameters);
    case 73:
        return func3_213C9C4(session, bot_slot, context,
                  parameters);
    case 75:
        return func3_213C88C(session, bot_slot, context,
                  parameters);
    case 77:
        return func3_213C75C(session, bot_slot, context,
                  parameters);
    case 86:
        return func3_213C0D0(session, bot_slot, context,
                  parameters);
    case 91:
        return func3_213BED8(session, bot_slot, context,
                  parameters);
    case 92:
        return func3_213BEBC(session, bot_slot, context,
                  parameters);
    case 93:
        return func3_213BEA0(session, bot_slot, context,
                  parameters);
    case 94:
        return func3_213BE48(session, bot_slot, context,
                  parameters);
    case 134:
        return func3_213B1D8(session, bot_slot, context,
                  parameters);
    case 135:
        return func3_213B1C0(session, bot_slot, context,
                  parameters);
    case 136:
        return func3_213B1A8(session, bot_slot, context,
                  parameters);
    case 137:
        return func3_213B190(session, bot_slot, context,
                  parameters);
    case 138:
        return func3_213B178(session, bot_slot, context,
                  parameters);
    case 139:
        return func3_213B160(session, bot_slot, context,
                  parameters);
    case 140:
        return func3_213B148(session, bot_slot, context,
                  parameters);
    case 141:
        return func3_213B130(session, bot_slot, context,
                  parameters);
    case 142:
        return func3_213B118(session, bot_slot, context,
                  parameters);
    case 143:
        return func3_213B100(session, bot_slot, context,
                  parameters);
    case 144:
        return func3_213B0E8(session, bot_slot, context,
                  parameters);
    case 145:
        return func3_213B0D0(session, bot_slot, context,
                  parameters);
    case 146:
        return func3_213B0B8(session, bot_slot, context,
                  parameters);
    case 147:
        return func3_213B0A0(session, bot_slot, context,
                  parameters);
    case 148:
        return func3_213B088(session, bot_slot, context,
                  parameters);
    case 149:
        return func3_213B070(session, bot_slot, context,
                  parameters);
    case 150:
        return func3_213B058(session, bot_slot, context,
                  parameters);
    case 151:
        return func3_213B040(session, bot_slot, context,
                  parameters);
    case 152:
        return func3_213B020(session, bot_slot, context,
                  parameters);
    case 153:
        return func3_213B000(session, bot_slot, context,
                  parameters);
    case 154:
        return func3_213AFE0(session, bot_slot, context,
                  parameters);
    case 155:
        return func3_213AFC0(session, bot_slot, context,
                  parameters);
    case 156:
        return func3_213AFA0(session, bot_slot, context,
                  parameters);
    case 157:
        return func3_213AF80(session, bot_slot, context,
                  parameters);
    case 158:
        return func3_213AF68(session, bot_slot, context,
                  parameters);
    case 159:
        return func3_213AF50(session, bot_slot, context,
                  parameters);
    case 160:
        return func3_213AF38(session, bot_slot, context,
                  parameters);
    case 161:
        return func3_213AF20(session, bot_slot, context,
                  parameters);
    case 162:
        return func3_213AF08(session, bot_slot, context,
                  parameters);
    case 163:
        return func3_213AEF0(session, bot_slot, context,
                  parameters);
    case 164:
        return func3_213AED8(session, bot_slot, context,
                  parameters);
    case 165:
        return func3_213AEC0(session, bot_slot, context,
                  parameters);
    case 166:
        return func3_213AEA8(session, bot_slot, context,
                  parameters);
    case 167:
        return func3_213AE90(session, bot_slot, context,
                  parameters);
    case 168:
        return func3_213AE78(session, bot_slot, context,
                  parameters);
    case 169:
        return func3_213AE60(session, bot_slot, context,
                  parameters);
    case 170:
        return func3_213AE48(session, bot_slot, context,
                  parameters);
    case 171:
        return func3_213AE30(session, bot_slot, context,
                  parameters);
    case 172:
        return func3_213AE14(session, bot_slot, context,
                  parameters);
    case 173:
        return func3_213ADF8(session, bot_slot, context,
                  parameters);
    case 176:
        return func3_213AD88(session, bot_slot, context,
                  parameters);
    case 177:
        return func3_213AD64(session, bot_slot, context,
                  parameters);
    case 179:
        return func3_213ACCC(session, bot_slot, context,
                  parameters);
    case 180:
        return func3_213ACA8(session, bot_slot, context,
                  parameters);
    case 181:
        return func3_213AC8C(session, bot_slot, context,
                  parameters);
    case 182:
        return func3_213AC70(session, bot_slot, context,
                  parameters);
    case 183:
        return func3_213AC54(session, bot_slot, context,
                  parameters);
    case 184:
        return func3_213AC38(session, bot_slot, context,
                  parameters);
    case 195:
        return func3_213A938(session, bot_slot, context,
                  parameters);
    case 196:
        return func3_213A91C(session, bot_slot, context,
                  parameters);
    case 209:
        return func3_213A688(session, bot_slot, context,
                  parameters);
    case 210:
        return func3_213A660(session, bot_slot, context,
                  parameters);
    case 211:
        return func3_213A650(session, bot_slot, context,
                  parameters);
    default: break;
    }
    handled = false;
    return 0;
}

} // namespace fruityprime::players
