#include "NetProtocol.hpp"

#include <algorithm>
#include <bit>
#include <cstddef>
#include <stdexcept>
#include <string_view>
#include <utility>

namespace { [[noreturn]] void R() { throw std::out_of_range("Index was outside the bounds of the array."); } template <typename T> T& At(std::span<T> span, std::size_t index) { if (index >= span.size()) { R(); } return span[index]; } template <typename T> const T& At(std::span<const T> span, std::size_t index) { if (index >= span.size()) { R(); } return span[index]; } template <typename T> std::span<T> Slice(std::span<T> span, std::size_t offset) { if (offset > span.size()) { R(); } return span.subspan(offset); } template <typename T> std::span<const T> Slice(std::span<const T> span, std::size_t offset) { if (offset > span.size()) { R(); } return span.subspan(offset); } template <typename T> std::span<T> Slice(std::span<T> span, std::size_t offset, std::size_t count) { if (offset > span.size() || count > span.size() - offset) { R(); } return span.subspan(offset, count); } template <typename T> std::span<const T> Slice(std::span<const T> span, std::size_t offset, std::size_t count) { if (offset > span.size() || count > span.size() - offset) { R(); } return span.subspan(offset, count); } template <typename T> void L(std::span<T> span, std::size_t count) { if (span.size() < count) { R(); } } template <typename T> void L(std::span<const T> span, std::size_t count) { if (span.size() < count) { R(); } } void Clear(std::span<std::uint8_t> span) { std::fill(span.begin(), span.end(), static_cast<std::uint8_t>(0)); } void CP(std::span<std::uint8_t> span, std::size_t count) { Clear(Slice(span, 0, count)); } void W16(std::span<std::uint8_t> destination, std::uint16_t value) { L(destination, 2); destination[0] = static_cast<std::uint8_t>(value); destination[1] = static_cast<std::uint8_t>(value >> 8); } std::uint16_t R16(std::span<const std::uint8_t> source) { L(source, 2); const std::uint16_t b0 = source[0]; const std::uint16_t b1 = source[1]; return static_cast<std::uint16_t>(b0 | static_cast<std::uint16_t>(b1 << 8)); } void WI16(std::span<std::uint8_t> destination, std::int16_t value) { W16(destination, static_cast<std::uint16_t>(value)); } std::int16_t RI16(std::span<const std::uint8_t> source) { return static_cast<std::int16_t>(R16(source)); } void W32(std::span<std::uint8_t> destination, std::uint32_t value) { L(destination, 4); destination[0] = static_cast<std::uint8_t>(value); destination[1] = static_cast<std::uint8_t>(value >> 8); destination[2] = static_cast<std::uint8_t>(value >> 16); destination[3] = static_cast<std::uint8_t>(value >> 24); } std::uint32_t R32(std::span<const std::uint8_t> source) { L(source, 4); return static_cast<std::uint32_t>(source[0]) | (static_cast<std::uint32_t>(source[1]) << 8) | (static_cast<std::uint32_t>(source[2]) << 16) | (static_cast<std::uint32_t>(source[3]) << 24); } void WB32(std::span<std::uint8_t> destination, std::uint32_t value) { L(destination, 4); destination[0] = static_cast<std::uint8_t>(value >> 24); destination[1] = static_cast<std::uint8_t>(value >> 16); destination[2] = static_cast<std::uint8_t>(value >> 8); destination[3] = static_cast<std::uint8_t>(value); } std::uint32_t RB32(std::span<const std::uint8_t> source) { L(source, 4); return (static_cast<std::uint32_t>(source[0]) << 24) | (static_cast<std::uint32_t>(source[1]) << 16) | (static_cast<std::uint32_t>(source[2]) << 8) | static_cast<std::uint32_t>(source[3]); } void WF(std::span<std::uint8_t> destination, float value) { W32(destination, std::bit_cast<std::uint32_t>(value)); } float RF(std::span<const std::uint8_t> source) { return std::bit_cast<float>(R32(source)); } std::vector<std::uint16_t> U16(std::string_view utf8) { std::vector<std::uint16_t> result; result.reserve(utf8.size()); std::size_t index = 0; while (index < utf8.size()) { const auto first = static_cast<unsigned char>(utf8[index]); std::uint32_t codePoint = 0xFFFDU; std::size_t consumed = 1; if (first < 0x80U) { codePoint = first; } else if (first >= 0xC2U && first <= 0xDFU && index + 1 < utf8.size()) { const auto b1 = static_cast<unsigned char>(utf8[index + 1]); if ((b1 & 0xC0U) == 0x80U) { codePoint = (static_cast<std::uint32_t>(first & 0x1FU) << 6) | static_cast<std::uint32_t>(b1 & 0x3FU); consumed = 2; } } else if (first >= 0xE0U && first <= 0xEFU && index + 2 < utf8.size()) { const auto b1 = static_cast<unsigned char>(utf8[index + 1]); const auto b2 = static_cast<unsigned char>(utf8[index + 2]); const bool continuation = (b1 & 0xC0U) == 0x80U && (b2 & 0xC0U) == 0x80U; const bool notOverlong = first != 0xE0U || b1 >= 0xA0U; const bool notSurrogate = first != 0xEDU || b1 <= 0x9FU; if (continuation && notOverlong && notSurrogate) { codePoint = (static_cast<std::uint32_t>(first & 0x0FU) << 12) | (static_cast<std::uint32_t>(b1 & 0x3FU) << 6) | static_cast<std::uint32_t>(b2 & 0x3FU); consumed = 3; } } else if (first >= 0xF0U && first <= 0xF4U && index + 3 < utf8.size()) { const auto b1 = static_cast<unsigned char>(utf8[index + 1]); const auto b2 = static_cast<unsigned char>(utf8[index + 2]); const auto b3 = static_cast<unsigned char>(utf8[index + 3]); const bool continuation = (b1 & 0xC0U) == 0x80U && (b2 & 0xC0U) == 0x80U && (b3 & 0xC0U) == 0x80U; const bool notOverlong = first != 0xF0U || b1 >= 0x90U; const bool inRange = first != 0xF4U || b1 <= 0x8FU; if (continuation && notOverlong && inRange) { codePoint = (static_cast<std::uint32_t>(first & 0x07U) << 18) | (static_cast<std::uint32_t>(b1 & 0x3FU) << 12) | (static_cast<std::uint32_t>(b2 & 0x3FU) << 6) | static_cast<std::uint32_t>(b3 & 0x3FU); consumed = 4; } } if (codePoint <= 0xFFFFU) { result.push_back(static_cast<std::uint16_t>(codePoint)); } else { const std::uint32_t scalar = codePoint - 0x10000U; result.push_back(static_cast<std::uint16_t>(0xD800U + (scalar >> 10))); result.push_back(static_cast<std::uint16_t>(0xDC00U + (scalar & 0x3FFU))); } index += consumed; } return result; } std::string DA(std::span<const std::uint8_t> source) { std::string result; result.reserve(source.size()); for (const std::uint8_t value : source) { result.push_back(value <= 0x7FU ? static_cast<char>(value) : '?'); } return result; } template <typename T> const T& AA(const std::shared_ptr<std::vector<T>>& array, std::size_t index) { if (array == nullptr) { throw System::NullReferenceException(); } if (index >= array->size()) { R(); } return (*array)[index]; } template <typename T> T& AAM(const std::shared_ptr<std::vector<T>>& array, std::size_t index) { if (array == nullptr) { throw System::NullReferenceException(); } if (index >= array->size()) { R(); } return (*array)[index]; } } namespace MphRead::Mods::Network { void RefusedPacket::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = Reason; At(dest, 1) = Players; At(dest, 2) = MaxPlayers; } RefusedPacket RefusedPacket::Read(std::span<const std::uint8_t> src) { RefusedPacket packet; packet.Reason = At(src, 0); packet.Players = src.size() > 1 ? src[1] : static_cast<std::uint8_t>(0); packet.MaxPlayers = src.size() > 2 ? src[2] : static_cast<std::uint8_t>(0); return packet; } std::string RefusedPacket::Describe(const std::optional<std::string>& where) const { const std::string prefix = where.value_or(""); if (Reason == ReasonFull) { return prefix + " is full (" + std::to_string(static_cast<unsigned int>(Players)) + "/" + std::to_string(static_cast<unsigned int>(MaxPlayers)) + " players). Try again when somebody leaves."; } if (Reason == ReasonProtocol) { return prefix + " is running a different version of the game. " + "One of you needs updating."; } return prefix + " would not admit this client."; } void HostRequestPacket::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = Protocol; At(dest, 1) = MaxPlayers; At(dest, 2) = Mode; W16(Slice(dest, 3), TimeLimit); W16(Slice(dest, 5), PointGoal); NetText::Write(Slice(dest, 7, MaxRoomBytes), RoomKey); NetText::Write(Slice(dest, 7 + MaxRoomBytes, MaxNameBytes), ServerName); } HostRequestPacket HostRequestPacket::Read(std::span<const std::uint8_t> src) { HostRequestPacket packet; packet.Protocol = At(src, 0); packet.MaxPlayers = At(src, 1); packet.Mode = At(src, 2); packet.TimeLimit = R16(Slice(src, 3)); packet.PointGoal = R16(Slice(src, 5)); packet.RoomKey = NetText::Read(Slice(src, 7, MaxRoomBytes)); packet.ServerName = NetText::Read(Slice(src, 7 + MaxRoomBytes, MaxNameBytes)); return packet; } void HostReplyPacket::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = Started ? static_cast<std::uint8_t>(1) : static_cast<std::uint8_t>(0); W16(Slice(dest, 1), Port); NetText::Write(Slice(dest, 3, MaxReasonBytes), Reason); } HostReplyPacket HostReplyPacket::Read(std::span<const std::uint8_t> src) { HostReplyPacket packet; packet.Started = At(src, 0) != 0; packet.Port = R16(Slice(src, 1)); packet.Reason = NetText::Read(Slice(src, 3, MaxReasonBytes)); return packet; } void NetText::Write(std::span<std::uint8_t> dest, const std::optional<std::string>& value) { Clear(dest); if (!value.has_value() || value->empty()) { return; } const std::vector<std::uint16_t> units = U16(*value); const std::size_t count = std::min(units.size(), dest.size()); for (std::size_t index = 0; index < count; index++) { const std::uint16_t unit = units[index]; dest[index] = unit < 32U || unit > 126U ? static_cast<std::uint8_t>('?') : static_cast<std::uint8_t>(unit); } } std::string NetText::Read(std::span<const std::uint8_t> src) { std::size_t length = 0; while (length < src.size() && src[length] != 0) { length++; } return length == 0 ? std::string{} : DA(Slice(src, 0, length)); } void MasterEntryPacket::Write(std::span<std::uint8_t> dest) const { WB32(dest, Address); W16(Slice(dest, 4), Port); At(dest, 6) = Players; At(dest, 7) = MaxPlayers; At(dest, 8) = Mode; At(dest, 9) = Protocol; NetText::Write(Slice(dest, 10, MaxNameBytes), ServerName); NetText::Write(Slice(dest, 10 + MaxNameBytes, MaxRoomBytes), RoomKey); } MasterEntryPacket MasterEntryPacket::Read(std::span<const std::uint8_t> src) { MasterEntryPacket packet; packet.Address = RB32(src); packet.Port = R16(Slice(src, 4)); packet.Players = At(src, 6); packet.MaxPlayers = At(src, 7); packet.Mode = At(src, 8); packet.Protocol = At(src, 9); packet.ServerName = NetText::Read(Slice(src, 10, MaxNameBytes)); packet.RoomKey = NetText::Read(Slice(src, 10 + MaxNameBytes, MaxRoomBytes)); return packet; } void MasterHeartbeatPacket::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = Protocol; W16(Slice(dest, 1), Port); At(dest, 3) = Players; At(dest, 4) = MaxPlayers; At(dest, 5) = Mode; NetText::Write(Slice(dest, 6, MasterEntryPacket::MaxNameBytes), ServerName); NetText::Write(Slice(dest, 6 + MasterEntryPacket::MaxNameBytes, MasterEntryPacket::MaxRoomBytes), RoomKey); } MasterHeartbeatPacket MasterHeartbeatPacket::Read(std::span<const std::uint8_t> src) { MasterHeartbeatPacket packet; packet.Protocol = At(src, 0); packet.Port = R16(Slice(src, 1)); packet.Players = At(src, 3); packet.MaxPlayers = At(src, 4); packet.Mode = At(src, 5); packet.ServerName = NetText::Read(Slice(src, 6, MasterEntryPacket::MaxNameBytes)); packet.RoomKey = NetText::Read(Slice(src, 6 + MasterEntryPacket::MaxNameBytes, MasterEntryPacket::MaxRoomBytes)); return packet; } bool MatchStatePacket::Ending() const noexcept { return (Flags & FlagEnding) != 0; } bool MatchStatePacket::FriendlyFire() const noexcept { return (Flags & FlagFriendlyFire) != 0; } bool MatchStatePacket::ShadowFreeze() const noexcept { return (Flags & FlagNoShadowFreeze) == 0; } void MatchStatePacket::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = Mode; WF(Slice(dest, 1), TimeRemaining); WF(Slice(dest, 5), TimeElapsed); At(dest, 9) = PlayerCount; At(dest, 10) = Flags; W16(Slice(dest, 11), PointGoal); W16(Slice(dest, 13), MatchId); WriteName(Slice(dest, 15), RoomKey); WriteName(Slice(dest, 15 + MaxNameBytes), NextRoomKey); } MatchStatePacket MatchStatePacket::Read(std::span<const std::uint8_t> src) { MatchStatePacket packet; packet.Mode = At(src, 0); packet.TimeRemaining = RF(Slice(src, 1)); packet.TimeElapsed = RF(Slice(src, 5)); packet.PlayerCount = At(src, 9); packet.Flags = At(src, 10); packet.PointGoal = R16(Slice(src, 11)); packet.MatchId = R16(Slice(src, 13)); packet.RoomKey = ReadName(Slice(src, 15)); packet.NextRoomKey = ReadName(Slice(src, 15 + MaxNameBytes)); return packet; } void MatchStatePacket::WriteName(std::span<std::uint8_t> dest, const std::optional<std::string>& value) { CP(dest, MaxNameBytes); if (!value.has_value() || value->empty()) { return; } const std::vector<std::uint16_t> units = U16(*value); const std::size_t count = std::min<std::size_t>(units.size(), MaxNameBytes); for (std::size_t index = 0; index < count; index++) { dest[index] = static_cast<std::uint8_t>(units[index]); } } std::string MatchStatePacket::ReadName(std::span<const std::uint8_t> src) { std::size_t length = 0; while (length < static_cast<std::size_t>(MaxNameBytes) && At(src, length) != 0) { length++; } return length == 0 ? std::string{} : DA(Slice(src, 0, length)); } void ServerStatusPacket::Write(std::span<std::uint8_t> dest) const { Match.Write(dest); At(dest, MatchStatePacket::Size) = MaxPlayers; At(dest, MatchStatePacket::Size + 1) = Protocol; NetText::Write(Slice(dest, MatchStatePacket::Size + 2, MaxNameBytes), ServerName); } ServerStatusPacket ServerStatusPacket::Read(std::span<const std::uint8_t> src) { ServerStatusPacket packet; packet.Match = MatchStatePacket::Read(src); packet.MaxPlayers = At(src, MatchStatePacket::Size); packet.Protocol = At(src, MatchStatePacket::Size + 1); packet.ServerName = src.size() >= static_cast<std::size_t>(Size) ? std::optional<std::string>( NetText::Read(Slice(src, MatchStatePacket::Size + 2, MaxNameBytes))) : std::optional<std::string>(std::string{}); return packet; } RosterPacket RosterPacket::Create() { RosterPacket packet; packet.Count = 0; packet.Slots = std::make_shared<std::vector<std::uint8_t>>(MaxSlots); packet.Hunters = std::make_shared<std::vector<std::uint8_t>>(MaxSlots); packet.Colors = std::make_shared<std::vector<std::uint8_t>>(MaxSlots); packet.Pings = std::make_shared<std::vector<std::uint16_t>>(MaxSlots); packet.Names = std::make_shared<std::vector<std::optional<std::string>>>(MaxSlots); return packet; } void RosterPacket::Write(std::span<std::uint8_t> dest) const { CP(dest, Size); dest[0] = Count; std::size_t offset = 1; for (std::size_t index = 0; index < static_cast<std::size_t>(Count) && index < static_cast<std::size_t>(MaxSlots); index++) { dest[offset] = AA(Slots, index); dest[offset + 1] = AA(Hunters, index); dest[offset + 2] = AA(Colors, index); W16(Slice(dest, offset + 3), AA(Pings, index)); WriteName(Slice(dest, offset + 5, MaxNameBytes), AA(Names, index)); offset += EntrySize; } } RosterPacket RosterPacket::Read(std::span<const std::uint8_t> src) { RosterPacket packet = Create(); packet.Count = std::min<std::uint8_t>(At(src, 0), static_cast<std::uint8_t>(MaxSlots)); std::size_t offset = 1; for (std::size_t index = 0; index < packet.Count; index++) { AAM(packet.Slots, index) = At(src, offset); AAM(packet.Hunters, index) = At(src, offset + 1); AAM(packet.Colors, index) = At(src, offset + 2); AAM(packet.Pings, index) = R16(Slice(src, offset + 3)); AAM(packet.Names, index) = ReadName(Slice(src, offset + 5, MaxNameBytes)); offset += EntrySize; } return packet; } void RosterPacket::WriteName(std::span<std::uint8_t> dest, const std::optional<std::string>& value) { Clear(dest); if (!value.has_value() || value->empty()) { return; } const std::vector<std::uint16_t> units = U16(*value); const std::size_t count = std::min<std::size_t>(units.size(), MaxNameBytes); for (std::size_t index = 0; index < count; index++) { const std::uint16_t unit = units[index]; dest[index] = unit < 32U || unit > 126U ? static_cast<std::uint8_t>('?') : static_cast<std::uint8_t>(unit); } } std::string RosterPacket::ReadName(std::span<const std::uint8_t> src) { std::size_t length = 0; while (length < src.size() && src[length] != 0) { length++; } return length == 0 ? std::string{} : DA(Slice(src, 0, length)); } void ChatPacket::Write(std::span<std::uint8_t> dest) const { CP(dest, Size); dest[0] = Slot; dest[1] = Kind; WriteAscii(Slice(dest, 2, MaxNameBytes), Name); WriteAscii(Slice(dest, 2 + MaxNameBytes, MaxTextBytes), Text); } ChatPacket ChatPacket::Read(std::span<const std::uint8_t> src) { ChatPacket packet; packet.Slot = At(src, 0); packet.Kind = At(src, 1); packet.Name = ReadAscii(Slice(src, 2, MaxNameBytes)); packet.Text = ReadAscii(Slice(src, 2 + MaxNameBytes, MaxTextBytes)); return packet; } void ChatPacket::WriteAscii(std::span<std::uint8_t> dest, const std::optional<std::string>& value) { Clear(dest); if (!value.has_value() || value->empty()) { return; } const std::vector<std::uint16_t> units = U16(*value); const std::size_t count = std::min(units.size(), dest.size()); for (std::size_t index = 0; index < count; index++) { const std::uint16_t unit = units[index]; dest[index] = unit < 32U || unit > 126U ? static_cast<std::uint8_t>('?') : static_cast<std::uint8_t>(unit); } } std::string ChatPacket::ReadAscii(std::span<const std::uint8_t> src) { std::size_t length = 0; while (length < src.size() && src[length] != 0) { length++; } if (length == 0) { return {}; } std::string result; result.reserve(length); for (std::size_t index = 0; index < length; index++) { const std::uint8_t value = src[index]; result.push_back(value < 32U || value > 126U ? '?' : static_cast<char>(value)); } return result; } void IntentPacket::Write(std::span<std::uint8_t> dest) const { W32(Slice(dest, 0), Frame); W32(Slice(dest, 4), static_cast<std::uint32_t>(Buttons)); WF(Slice(dest, 8), Aim.X); WF(Slice(dest, 12), Aim.Y); WF(Slice(dest, 16), Aim.Z); At(dest, 20) = WeaponSelect; for (std::size_t index = 0; index < static_cast<std::size_t>(PressHistory); index++) { std::uint32_t value = 0; if (Presses != nullptr && index < Presses->size()) { value = (*Presses)[index]; } W32(Slice(dest, 21 + index * 4), value); } constexpr std::size_t at = 21 + PressHistory * 4; WF(Slice(dest, at), Position.X); WF(Slice(dest, at + 4), Position.Y); WF(Slice(dest, at + 8), Position.Z); W16(Slice(dest, at + 12), AmmoUa); W16(Slice(dest, at + 14), AmmoMissiles); W32(Slice(dest, at + 16), AckFrame); } IntentPacket IntentPacket::Read(std::span<const std::uint8_t> src) { auto presses = std::make_shared<std::vector<std::uint32_t>>(PressHistory); for (std::size_t index = 0; index < static_cast<std::size_t>(PressHistory); index++) { (*presses)[index] = R32(Slice(src, 21 + index * 4)); } IntentPacket packet; packet.Frame = R32(Slice(src, 0)); packet.Buttons = static_cast<IntentButtons>(R32(Slice(src, 4))); packet.Aim = ::OpenTK::Mathematics::Vector3( RF(Slice(src, 8)), RF(Slice(src, 12)), RF(Slice(src, 16))); packet.WeaponSelect = At(src, 20); packet.Presses = std::move(presses); packet.Position = ::OpenTK::Mathematics::Vector3( RF(Slice(src, 21 + PressHistory * 4)), RF(Slice(src, 25 + PressHistory * 4)), RF(Slice(src, 29 + PressHistory * 4))); packet.AmmoUa = R16(Slice(src, 33 + PressHistory * 4)); packet.AmmoMissiles = R16(Slice(src, 35 + PressHistory * 4)); packet.AckFrame = R32(Slice(src, 37 + PressHistory * 4)); return packet; } void PlayerState::Write(std::span<std::uint8_t> dest) const { At(dest, 0) = SlotIndex; At(dest, 1) = Flags; WriteVec(Slice(dest, 2), Position); WriteVec(Slice(dest, 14), Speed); WriteVec(Slice(dest, 26), Facing); W16(Slice(dest, 38), Health); At(dest, 40) = CurrentWeapon; At(dest, 41) = Team; At(dest, 42) = DamageSeq; At(dest, 43) = AttackerSlot; At(dest, 44) = DamageBeam; At(dest, 45) = DamageFlags; WriteVec(Slice(dest, 46), HitDirection); WI16(Slice(dest, 58), Points); W16(Slice(dest, 60), Kills); W16(Slice(dest, 62), Deaths); } PlayerState PlayerState::Read(std::span<const std::uint8_t> src) { PlayerState packet; packet.SlotIndex = At(src, 0); packet.Flags = At(src, 1); packet.Position = ReadVec(Slice(src, 2)); packet.Speed = ReadVec(Slice(src, 14)); packet.Facing = ReadVec(Slice(src, 26)); packet.Health = R16(Slice(src, 38)); packet.CurrentWeapon = At(src, 40); packet.Team = At(src, 41); packet.DamageSeq = At(src, 42); packet.AttackerSlot = At(src, 43); packet.DamageBeam = At(src, 44); packet.DamageFlags = At(src, 45); packet.HitDirection = ReadVec(Slice(src, 46)); packet.Points = RI16(Slice(src, 58)); packet.Kills = R16(Slice(src, 60)); packet.Deaths = R16(Slice(src, 62)); return packet; } void PlayerState::WriteVec(std::span<std::uint8_t> dest, ::OpenTK::Mathematics::Vector3 value) { WF(Slice(dest, 0), value.X); WF(Slice(dest, 4), value.Y); WF(Slice(dest, 8), value.Z); } ::OpenTK::Mathematics::Vector3 PlayerState::ReadVec(std::span<const std::uint8_t> src) { return ::OpenTK::Mathematics::Vector3( RF(Slice(src, 0)), RF(Slice(src, 4)), RF(Slice(src, 8))); } void SnapshotHeader::Write(std::span<std::uint8_t> dest) const { W32(Slice(dest, 0), Frame); W32(Slice(dest, 4), Rng1); W32(Slice(dest, 8), Rng2); At(dest, 12) = PlayerCount; } SnapshotHeader SnapshotHeader::Read(std::span<const std::uint8_t> src) { SnapshotHeader packet; packet.Frame = R32(Slice(src, 0)); packet.Rng1 = R32(Slice(src, 4)); packet.Rng2 = R32(Slice(src, 8)); packet.PlayerCount = At(src, 12); return packet; } void VotePacket::Write(std::span<std::uint8_t> dest) const { CP(dest, Size); dest[0] = Kind; ChatPacket::WriteAscii(Slice(dest, 1, MaxRoomBytes), RoomKey); } VotePacket VotePacket::Read(std::span<const std::uint8_t> src) { VotePacket packet; packet.Kind = At(src, 0); packet.RoomKey = ChatPacket::ReadAscii(Slice(src, 1, MaxRoomBytes)); return packet; } void VoteStatePacket::Write(std::span<std::uint8_t> dest) const { CP(dest, Size); dest[0] = State; ChatPacket::WriteAscii(Slice(dest, 1, MaxRoomBytes), RoomKey); ChatPacket::WriteAscii(Slice(dest, 1 + MaxRoomBytes, MaxNameBytes), Proposer); constexpr std::size_t at = 1 + MaxRoomBytes + MaxNameBytes; dest[at] = Yes; dest[at + 1] = No; dest[at + 2] = Eligible; dest[at + 3] = Needed; W16(Slice(dest, at + 4, 2), Seconds); } VoteStatePacket VoteStatePacket::Read(std::span<const std::uint8_t> src) { constexpr std::size_t at = 1 + MaxRoomBytes + MaxNameBytes; VoteStatePacket packet; packet.State = At(src, 0); packet.RoomKey = ChatPacket::ReadAscii(Slice(src, 1, MaxRoomBytes)); packet.Proposer = ChatPacket::ReadAscii(Slice(src, 1 + MaxRoomBytes, MaxNameBytes)); packet.Yes = At(src, at); packet.No = At(src, at + 1); packet.Eligible = At(src, at + 2); packet.Needed = At(src, at + 3); packet.Seconds = R16(Slice(src, at + 4, 2)); return packet; } }
